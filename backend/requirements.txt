# --- Core API ---
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart
email-validator
requests

# --- Database ---
SQLAlchemy>=2.0.25
psycopg2-binary>=2.9.9

# --- Security (CRITICAL FIXES) ---
# Removed [bcrypt] from passlib to prevent version conflicts
passlib
python-jose[cryptography]
# Pin bcrypt to 4.0.1 to fix "password longer than 72 bytes" error
bcrypt==4.0.1
cryptography>=41.0.0

# --- Data Science & AI ---
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
scipy>=1.11.0
xgboost>=2.0.3
shap>=0.44.0
joblib>=1.3.0

# --- NLP (HuggingFace) ---
transformers>=4.30.0
tokenizers>=0.13.0
torch>=2.0.0

# --- Utilities ---
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-dateutil
pytz
tqdm